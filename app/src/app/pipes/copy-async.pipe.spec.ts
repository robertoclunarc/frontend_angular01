import { CopyAsyncPipe } from './copy-async.pipe';

describe('CopyAsyncPipe', () => {
  it('create an instance', () => {
    const pipe = new CopyAsyncPipe();
    expect(pipe).toBeTruthy();
  });
});
